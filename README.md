[# typing_test](https://lihsus19.github.io/typing_test/) note this is not going to work!
Project: FastFinger Typing System

Setup Instructions:

1. Install Node.js (https://nodejs.org/en)
    1.1 Check in terminal write node -v and run
2. Open terminal in project folder
3. Run: npm install
4. Run: npm run dev
5. Open browser at: http://localhost:3000

Login:
- You can create an account or use guest mode

Notes:
- MongoDB connection required (see .env file)

Git hub Update

git add .
git commit -m "update"
git push